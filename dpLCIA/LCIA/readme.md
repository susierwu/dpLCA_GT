## to create LCIA: only for BW2, not for BW25 

## DO NOT RUN: 

- all files other than  **v2026** in the name are old models (metrics calculated with FaIR model, and IPCC analytical impulse response (ModuleB) in the FaIR folder)


## updated v2026 model: 
- the two notebooks with **v2026** in the name is used to create BW2 prospective GWP100 method, with the output BW2 datapacakge in the `pGWP100_bw2package` folder
- all other notebooks are replaced with new analysis under `AGWPCO2_dp_approach` and `AGWPCO2_fixed_approach` folders
- the new .nc data under `data/v2026` is prepared and copied from the `AGWPCO2_dp_approach` and `AGWPCO2_fixed_approach` folders
  

